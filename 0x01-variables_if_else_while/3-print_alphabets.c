#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
{
	char c;

        c = 'a';
	d = 'A';
        while
		(c <= 'z) {
		putchar(c);
		c++;
		}
	while
	       	(d <= 'Z') {
		putchar(c);
		d++;
		}
	putchar('\n');
	return (0);
}
